name: Org ADR Auto Sync

on:
  schedule:
    - cron: "7 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: org-adr-auto-sync-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync:
    name: org ADR auto-sync
    permissions:
      contents: read
      pull-requests: read
    uses: NWarila/.github/.github/workflows/reusable-org-adr-auto-sync.yaml@e8bc517828a9915a401096305124dff316b76644
    with:
      source_repo: NWarila/.github
      source_ref: main
      branch_name: sync/org-adrs
      pr_title: "[sync] refresh org ADR mirrors"
    secrets:
      sync_token: ${{ secrets.ORG_ADR_SYNC_TOKEN }}
